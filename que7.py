#program - use a stack to check if string of parantheses(e.g. "((()))") is balanced. 
# challenge : extend the program to support other types of parantheses like {} and [].

def parantheses_string():
    input_string = ("hellow world")
    list_string = list(input_string)
    dict_string = dict(input_string)
    print(list_string)
    print(dict_string)
parantheses_string()